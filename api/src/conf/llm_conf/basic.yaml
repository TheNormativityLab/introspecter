# check https://docs.litellm.ai/docs/routing for more details on how to configure multiple endpoints 

language_models:
  - model_name: "gpt-3.5-turbo"
    litellm_params:
      model: "gpt-3.5-turbo" # the actual model endpoint 
      timeout: 300
      # tpm: 100000
      # rpm: 1000
      num_retries: 3


completion_params:
  temperature: 0.0
  max_tokens: 300
  # etc etc.